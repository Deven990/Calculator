<resources>
    <!-- Base application theme. -->
    <style name="AppThemeDark" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppThemeLight" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorthemeD</item>
        <item name="colorPrimaryDark">@color/colorthemeDD</item>
        <item name="colorAccent">@color/colortemeAccent</item>
    </style>

    <style name="Widget.AppCompat.CompoundButton.Switch." parent="android:Widget.CompoundButton">
        <item name="android:background">@drawable/ic_toggle_theme_bg</item>
        <item name="android:disabledAlpha">?android:attr/disabledAlpha</item>
    </style>

    <style name="toggleButton"  parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:switchStyle">@style/Widget.AppCompat.CompoundButton.Switch.</item>
    </style>

</resources>